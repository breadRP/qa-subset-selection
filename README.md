# kNN 기반 희귀도 추정을 이용해 데이터 subset selection 문제를 정의하고 이를 QUBO 최적화 문제로 모델링하는 방법을 연구한다.

## 연구 주제
중요한 데이터셋을 선택하는 방법을 연구하고, QUBO(QA)를 이용하여 최적화 문제 모델링한다.

## 문제 정의
많은 데이터셋에는 서로 매우 유사하거나 중복되는 데이터가 포함되어 있는 경우가 많다. 이러한 데이터 중 일부는 모델 학습에 큰 영향을 주지 않을 수 있으며, 모든 데이터를 사용하는 것은 학습 효율 측면에서 비효율적일 수 있다.  

따라서 데이터셋에서 **정보성이 높은 데이터 샘플을 선택하는 문제(subset selection)**는 중요한 연구 주제이다.

## 아이디어

본 연구에서는 데이터 샘플의 희귀도(rarity)를 k번째 최근접 이웃(k-nearest neighbor) 거리를 이용하여 추정하고, cosine_similarity를 이용해 각 데이터 간 중복성을 추정한다. 또한 데이터 갯수의 제한조건은 일단 데이터가 너무 적게나, 많이 선택되지 않도록 s_i의 중간값으로 설정한다. 이를 기반으로 중요한 데이터를 선택하는 문제를 QUBO(Quadratic Unconstrained Binary Optimization) 형태의 최적화 문제로 모델링한다.

제안한 방법을 검증하기 위해, 먼저 키와 몸무게와 같은 간단한 feature로 구성된 데이터셋을 이용하여 알고리즘의 동작을 확인하고 이후 더 복잡한 데이터셋으로 확장하는 것을 목표로 한다.  

## 현재 진행도
- QUBO 구현 완료 및 랜덤 데이터 기준으로 작동 확인
- baseline과 비교 준비

## 코드 구성
src/  
├ s_i.cpp  
├ γ.cpp  
└ r_ij.py

