# KNN (k-Nearest Neighbors)

## 개요
**KNN (k-최근접 이웃 알고리즘)** 은 지도학습 알고리즘으로, 새로운 데이터가 들어왔을 때 기존 데이터 중 **가장 가까운 k개의 이웃**을 참고하여 결과를 예측하는 방법이다.  
핵심 아이디어는 **"가까운 데이터는 비슷한 특성을 가진다"** 는 가정이다.

---

## 동작 과정
1. 새로운 데이터 입력
2. 기존 데이터들과의 거리 계산
3. 가장 가까운 k개의 데이터 선택
4. 결과 예측
   - **분류** → 다수결 투표
   - **회귀** → 평균값 계산

---

## 거리 계산 방법

가장 많이 사용하는 거리 척도는 **유클리드 거리**이다.

\[
d(x,y)=\sqrt{\sum_{i=1}^{n}(x_i-y_i)^2}
\]

다른 거리 함수도 사용할 수 있다.

- 맨해튼 거리 (Manhattan Distance)
- 민코프스키 거리 (Minkowski Distance)
- 코사인 거리 (Cosine Distance)

---

## 주요 하이퍼파라미터

| 파라미터 | 설명 |
|--------|------|
| k | 참조할 이웃 개수 |
| metric | 거리 계산 방식 |
| weights | 이웃 가중치 방식 |

- **k 값이 작으면** → 노이즈 영향 큼 (과적합)
- **k 값이 크면** → 경계가 단순해짐 (과소적합)

---

## 장점
- 구현이 간단함
- 학습 과정이 거의 없음 (Lazy Learning)
- 복잡한 결정 경계 표현 가능

---

## 단점
- 데이터가 많으면 계산 비용 증가
- 특징 스케일 차이에 민감 → 정규화 필요
- 차원이 높아질수록 성능 저하 (차원의 저주)

---

## 활용 분야
- 추천 시스템
- 이미지 분류
- 텍스트 분류
- 이상치 탐지

---

## 한 줄 요약
> KNN은 새로운 데이터 주변의 이웃을 보고 결과를 결정하는 가장 직관적인 머신러닝 알고리즘이다.
